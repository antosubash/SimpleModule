@inherits LayoutComponentBase
@inject NavigationManager Navigation

<div class="max-w-4xl mx-auto">
    <div class="mb-8">
        <h1 class="text-3xl font-extrabold tracking-tight" style="font-family:'Sora',sans-serif;">
            <span class="gradient-text">Account Settings</span>
        </h1>
        <p class="text-text-muted text-sm mt-1.5">Manage your profile, security, and preferences</p>
    </div>

    <div class="flex flex-col sm:flex-row gap-6">
        <aside class="sm:w-56 shrink-0">
            <div class="glass-card p-3 sm:sticky sm:top-20">
                <ManageNav ActivePage="@GetActivePage()" />
            </div>
        </aside>
        <div class="flex-1 min-w-0">
            <div class="glass-card p-6 sm:p-8">
                @Body
            </div>
        </div>
    </div>
</div>

@code {
    private string GetActivePage()
    {
        var uri = new Uri(Navigation.Uri);
        var path = uri.AbsolutePath.TrimEnd('/');
        var lastSegment = path.Split('/').LastOrDefault() ?? "";
        return lastSegment == "Manage" ? "Index" : lastSegment;
    }
}
